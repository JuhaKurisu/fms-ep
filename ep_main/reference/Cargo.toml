[package]
name = "reference"
version = "0.1.0"
edition = "2024"
default-run = "reference"

[dependencies]
image = { version = "0.25", default-features = false, features = ["png", "hdr"] }
rayon = "1"
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[profile.release]
debug = 1
